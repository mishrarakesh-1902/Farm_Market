# Minimal production requirements for deployment
Django==5.1.3
gunicorn==21.2.0
psycopg2-binary==2.9.6
dj-database-url==2.1.0
python-dotenv==1.0.0
cloudinary==1.44.1
django-cloudinary-storage==0.3.0
django-widget-tweaks==1.5.0
Pillow==11.2.1
requests==2.32.3
python-json-logger==3.2.1
whitenoise==6.5.0
pymysql==1.1.0
gdown==5.1.0
# Optional / heavy packages (include only if your app requires ML or image processing)
# numpy==2.3.2
# scipy==1.15.3
# scikit-learn==1.6.1
# opencv-python==4.11.0.86

# NOTE: rpds-py and other Rust-backed packages may require a writable CARGO_HOME
# or prebuilt wheels. If you need them, either set CARGO_HOME=/tmp/.cargo in the
# build environment or add such packages to a separate dev/ci requirements file.